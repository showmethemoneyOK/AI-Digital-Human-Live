# AI数字人直播系统依赖包

# 核心依赖
aiohttp>=3.9.0
websockets>=12.0
requests>=2.31.0

# Web框架（TikTok Webhook需要）
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# 配置管理
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 数据处理
numpy>=1.24.0
pandas>=2.0.0

# 音频处理
pydub>=0.25.1
soundfile>=0.12.0

# 数据库
sqlite3>=3.0.0  # Python内置

# 日志和监控
structlog>=23.0.0
prometheus-client>=0.19.0

# 工具类
click>=8.1.0
rich>=13.0.0
tqdm>=4.65.0

# 开发依赖
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# 平台特定
# 抖音：需要额外配置
# TikTok：需要额外配置

# 注意：实际部署时可能需要根据平台选择安装
# pip install -r requirements.txt --no-deps  # 不安装开发依赖